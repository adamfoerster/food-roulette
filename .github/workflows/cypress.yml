name: End-to-end tests
on: [push]
jobs:
  cypress-run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v1

      - name: Cypress run
        uses: cypress-io/github-action@v1
        with:
          build: npm%20i%20%26%26%20npm%20i%20typescript%403.6
          start: npm start
          wait-on: http://localhost:4200
